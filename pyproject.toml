[project]
name = "splitwise-mcp"
version = "0.1.0"
description = "MCP server exposing essential Splitwise expense tools"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
