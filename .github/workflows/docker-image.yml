name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag registry.azeemwaqar.com/splitwise-mcp:$(date +%s)
    - name: Push the docker image
      run: docker push registry.azeemwaqar.com/splitwise-mcp:$(date +%s)
